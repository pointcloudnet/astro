'use strict';

const server_port = ':' + '3000';
const server_host = 'localhost';
const server_scheme = 'http' + '://';
const server_url = server_scheme + server_host + server_port;

export { server_url };